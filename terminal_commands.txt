# Topic: Execute Mongo Query Compiler
python -m src.services.query_compiler --intent intent.json --execute

# Topic: Load Neo4j Metadata
python -m scripts.load_neo4j --reset

# Topic: Generate Search Intent
python -m src.services.intent_service "Show oldest pending orders from Colombo in last month"